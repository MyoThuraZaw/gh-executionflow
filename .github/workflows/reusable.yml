name: Reusable Deploy
on: workflow_call                 # Event for reusable workflow
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Output information
        run: echo "Deploying & uploading ..."